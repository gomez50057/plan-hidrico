import SectionTitle from "@/components/shared/SectionTitle";

const TrainingSection = () => (
  <section>
    <SectionTitle title="Capacitación" />
    <p>Accede a los cursos y recursos...</p>
  </section>
);

export default TrainingSection;
