import SectionTitle from "@/components/shared/SectionTitle";

const PublicKnowledgeSection = () => (
  <section>
    <SectionTitle title="Conocimiento Público" />
    <p>Información disponible al público...</p>
  </section>
);

export default PublicKnowledgeSection;
